#include <bits/stdc++.h>
using namespace std;

int main(){

    unordered_set<int> st;  // unique elements , but not sorted
    // All operations - O(1), worst case - O(n)
    // lower_bound & upper_bound doent work

}