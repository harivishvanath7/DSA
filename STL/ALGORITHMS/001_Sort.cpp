#include <bits/stdc++.h>
using namespace std;

int main(){

    sort(a, a+n); // Full Sort

    sort(v.begin(), v.end()); // Full Sort

    sort(a+2, a+4); // Sort in range 

    sort(a, a+n, greater<int>); // descending order , greater<int> = comparator

}